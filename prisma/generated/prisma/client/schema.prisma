generator client {
  provider        = "prisma-client-js"
  output          = "./generated/prisma/client"
  previewFeatures = ["driverAdapters"] // Ubah ini
}

datasource db {
  provider = "postgresql"
  // Hapus baris url = env("DATABASE_URL")
}
